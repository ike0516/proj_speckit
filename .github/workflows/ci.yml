name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - run: echo "hello"
      - run: echo "my name is test,"
      - run: echo "how are you doing?"
